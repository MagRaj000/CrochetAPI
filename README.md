# Crochet API

To use the API you need to establish a database connection. This process is described in the main project [repository](https://github.com/MagRaj000/CrochetProject), alongside the API's functionality.


After correct set-up, you can send API calls on the standard port _localhost:8080/_ or use HTTP Request files in the project's **testApi** directory.
